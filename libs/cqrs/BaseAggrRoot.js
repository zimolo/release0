/**
 * Base class of aggregation root
 */
var aggrroot = function() {
	/**
	 * no implementation for now
	 * may contain logics common to all the aggregation roots
	 */

};

module.exports = aggrroot;